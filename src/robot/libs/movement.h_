#ifndef _MOVEMENT_H_
#define _MOVEMENT_H_


#include <kodama.h>

class CMovement
{
  public:
    CMovement();
    ~CMovement();


  void forward(float distance, float speed, int (CMovement::*term_func)() = nullptr);
  void rotate(float angle, float speed, int (CMovement::*term_func)() = nullptr);

  protected:
    int term_func_line_detected();
};

#endif
