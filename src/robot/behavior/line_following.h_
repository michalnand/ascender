#ifndef _LINE_FOLLOWING_H_
#define _LINE_FOLLOWING_H_

#include <config.h>
#include <kodama.h>
#include <libs.h>



class LineFollowing: public BEInterface
{
  private:
    Ticker ticker;

  private:
    class PID turn_pid;


  public:
    LineFollowing(BEngine &engine, bool precall_ = false);
    ~LineFollowing();

    bool condition();
    int action();
};

#endif
