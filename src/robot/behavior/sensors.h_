#ifndef _SENSORS_H_
#define _SENSORS_H_

#include <config.h>
#include <kodama.h>
#include <libs.h>


class Sensors: public BEInterface
{
  private:
    Ticker ticker_line_sensor;
    Ticker ticker_distance_sensor;
    Ticker ticker_imu_sensor;

  public:
    Sensors(BEngine &engine, bool precall_ = false);
    ~Sensors();

    bool condition();
    int action();
};

#endif
